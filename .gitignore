# Dependencies (none today — backstop)
node_modules/

# Workflow run artifacts
.security-scans/
.pr-reviews/
*.bak

# Secrets — never commit
.env
.env.*
.dev.vars

# OS / editor cruft
.DS_Store
*.log
